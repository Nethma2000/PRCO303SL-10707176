<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.careernextgen">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CareerNextGen">
        <activity android:name=".MathQuiz"></activity>
        <activity android:name=".RegisterAdvisors" />
        <activity android:name=".CareerAdvisorDashboard" />
        <activity android:name=".AdminDashboard" />
        <activity android:name=".StudentDashboard" />
        <activity android:name=".UserProfile" />
        <activity android:name=".Sign.VerifyOTP" />
        <activity android:name=".Sign.Login" />
        <activity android:name=".Walkthrough" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>